{
  "name": "bloodlink",
  "private": true,
  "version": "1.0.0",
  "description": "BloodLink - Serverless, ML-enhanced emergency blood donation and donor alert system (local recreation).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "reset-data": "node -e \"require('http').request('http://localhost:4000/api/reset',{method:'POST'},r=>r.on('data',()=>{}).on('end',()=>console.log('reset ok'))).end()\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
